<div class="publish">
	<div class="con">
		<%= form_for :blog, url: blog_edit_put_path(@blog), :method => :put, html: { id: 'editor' } do |f| %>
			<%= render partial: 'form', :object => f %>
		<% end %>
	</div>
</div>
<%= render partial: 'form_js' %>